# Fundamental Agent — Environment Configuration
# Copy this file to .env and fill in your API key

# ─── Required ───────────────────────────────────────────
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# ─── OpenRouter Fallback (activates automatically when Anthropic credits run out) ───
# Get your key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_key_here
# Analysis model — strong reasoning, ~$0.002 per fallback analysis
OPENROUTER_MODEL=google/gemma-4-31b-it
# Search model — Perplexity Sonar has real-time web search, ~$0.015 per fallback analysis
# Total fallback cost: ~$0.017 per full analysis  |  $5 lasts ~300 analyses
# No-cost alternative (stale data): google/gemma-4-26b-a4b-it:free
OPENROUTER_SEARCH_MODEL=perplexity/sonar

# ─── Optional ───────────────────────────────────────────
PORT=3001
NODE_ENV=development
